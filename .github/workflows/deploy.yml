name: Build and Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:  # Allows manual trigger from GitHub UI

env:
  APP_NAME: zuputo_admin_portal_20003
  DEPLOY_PATH: /var/www/zuputo/admin_portal
  NODE_VERSION: 24

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      
      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci --production
      
      # Create temporary .env.production for build process
      # This file will NOT be deployed to server
      - name: Create build-time env file
        run: |
          cat > .env.production << EOF
          NEXT_PUBLIC_API_BASE_URL=${{ secrets.NEXT_PUBLIC_API_BASE_URL }}
          NEXT_PUBLIC_PAYMENT_LINK_BASE_URL=${{ secrets.NEXT_PUBLIC_PAYMENT_LINK_BASE_URL }}
          EOF
      
      # Build reads .env.production and bakes NEXT_PUBLIC_* into bundle
      - name: Build application
        run: npm run build
      
      # Debug - Test SSH and directory access
      - name: Test directory access
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          script: |
            echo "Current user: $(whoami)"
            echo "Testing directory access..."
            ls -la /var/www/zuputo/admin_portal
            ls -la /var/www/zuputo/admin_portal/
            touch /var/www/zuputo/admin_portal/test_deploy.txt
            rm /var/www/zuputo/admin_portal/test_deploy.txt
            echo "Directory access successful!"
      
      # Deploy ONLY the built files to server
      - name: Deploy to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          source: ".next,public,package.json,package-lock.json"
          target: /var/www/zuputo/admin_portal
          strip_components: 0
          overwrite: true
      
      # Install dependencies and restart application on server
      - name: Install dependencies and restart
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: ${{ secrets.SERVER_HOST }}
          username: ${{ secrets.SERVER_USER }}
          key: ${{ secrets.SERVER_SSH_KEY }}
          port: ${{ secrets.SERVER_PORT }}
          command_timeout: 10m
          script: |
            cd ${{ env.DEPLOY_PATH }}
            npm ci --omit=dev
            sudo supervisorctl restart ${{ env.APP_NAME }}