# ============================================
# Production Environment Variables
# ============================================
# Copy this file to .env.production and fill in the actual values
# DO NOT commit .env.production to version control (it's in .gitignore)
#
# Alternatively, set these as environment variables in your deployment platform
# (Vercel, Railway, AWS, etc.) instead of using a file.
# ============================================

# ============================================
# Authentication (NextAuth)
# ============================================

# Secret key for signing/encrypting session tokens and cookies
# Generate with: openssl rand -base64 32
# IMPORTANT: Use a DIFFERENT secret for each environment (dev/staging/prod)
AUTH_SECRET=your_production_auth_secret_here

# Base URL where your admin portal is hosted in production
# Used by NextAuth to construct OAuth callback URLs
# Example: https://admin.zuputo.com
# IMPORTANT: Must match the redirect URI in your Google Cloud Console OAuth client
AUTH_URL=https://admin.zuputo.com

# ============================================
# Google OAuth Configuration
# ============================================

# Google OAuth 2.0 Client ID for production
# Create a separate OAuth client in Google Cloud Console for production
# Redirect URI must be: {AUTH_URL}/api/auth/callback/google
# IMPORTANT: Use DIFFERENT credentials for each environment
GOOGLE_CLIENT_ID=your_production_google_client_id_here

# Google OAuth 2.0 Client Secret for production
# Get this from your Google Cloud Console OAuth client
GOOGLE_CLIENT_SECRET=your_production_google_client_secret_here

# Email domain restriction (optional)
# Only users with emails from this domain can attempt to log in
# Example: zuputo.com
ALLOWED_DOMAIN=zuputo.com

# ============================================
# API Configuration
# ============================================

# Backend API base URL for production
# This is where your backend API server is hosted
# Example: https://api.zuputo.com
# Note: NEXT_PUBLIC_ prefix makes this available to client-side code
NEXT_PUBLIC_API_BASE_URL=https://api.zuputo.com

# ============================================
# Payment Links Configuration
# ============================================

# Base URL for customer-facing payment links
# This is where customers will be redirected to complete payments
# Example: https://payments.zuputo.com
# Payment links will be constructed as: {NEXT_PUBLIC_PAYMENT_LINK_BASE_URL}/adhoc-payments/{token}
# Note: NEXT_PUBLIC_ prefix makes this available to client-side code
NEXT_PUBLIC_PAYMENT_LINK_BASE_URL=https://payments.zuputo.com

